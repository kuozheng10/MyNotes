name: Sync MD to Notion

on:
  push:
    branches: [main]
    paths:
      - '**/*.md'
      - '!README.md'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install notion-client python-frontmatter

      - name: Sync changed MD files to Notion
        run: python scripts/sync_to_notion.py
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          SYNC_ALL: ${{ github.event_name == 'workflow_dispatch' && 'true' || 'false' }}
