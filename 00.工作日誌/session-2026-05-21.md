# Session 2026-05-21

## 完成事項

### 1. 個人資產管理系統 — MVP 架構設計與實作

設計並建立 `~/Documents/MyClaude/investment/` 整合系統，涵蓋：

**資料庫**
- SQLite schema (`db/schema.sql`)：accounts、positions、account_summaries、transactions、dividends、processed_emails
- `is_settled` 旗標：月帳單資料優先，自動蓋掉同月快照
- `db/init_db.py`：初始化 + 舊資料遷移

**環境設定**
- `env_utils.py`：統一讀 `~/.claude/env.sh`，`MY_ROC_ID` 作為安聯 PDF 密碼 fallback

**FirstTrade Parser**（`parsers/firsttrade.py`）
- 自動掃 Google Drive `MyClaude/First Trade/statement-*.pdf`
- Gemini 2.5-flash 解析：14 持倉 + 22 筆交易（4月帳單）
- 修 `_f()` helper 處理 VANECK "Not Available" → float 轉換不爆

**安聯基金 Processor**（`parsers/allianz.py` + `allianz_processor.py`）
- Gmail → 驗寄件人（prompt injection 防線）→ pikepdf 解密 → Gemini 解析
- 處理 2026 年 1–5 月共 5 封，累計 USD 102.64
- PDF 上傳 Google Drive，Gmail 加標籤「已處理-安聯」
- TG 推送配息通知

**Web App**（`web/`，Next.js + better-sqlite3，port 3099）
- `/`：總覽頁，各帳戶最新淨值 + 本月配息
- `/account/[id]`：3 tab（持倉 / 交易 / 配息）

### 2. Bug 修正

| 問題 | 修法 |
|------|------|
| Gemini 回傳 null → `float()` 爆 | 加 `_f()` helper |
| Gmail API `messageId=` 錯誤 | 改 `id=` |
| Drive 舊資料移動用 `id=` | 改 `fileId=` |
| 重複 error 記錄擋 retry | `DELETE FROM processed_emails WHERE result LIKE 'error:%'` |
| 舊 transactions 欄位不符 | Rename → 建新 → migrate |

### 3. Google Drive 路徑修正

- 安聯 PDF 原存 Drive 根目錄，修正為 `MyClaude/安聯對帳單`
- 移動既有 5 個 PDF 到正確位置
- `_get_or_create_drive_folder()` 改為在 `MYCLAUDE_FOLDER_ID` 下建立子資料夾

## 待辦

- Phase 2：基富通 Parser
- Phase 2：IB 盈透 Parser
- 即時匯率（USD/TWD 目前 hardcode 32.3）
- Web App 月份篩選 UI
- 晨報整合（用戶說之後再規劃）
