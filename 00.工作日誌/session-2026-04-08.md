---
date: 2026-04-08
tags: [工作日誌, sales-report, taishin, automation]
---

# Session 2026-04-08

## 完成事項

### 1. 台新信用卡 OCR 修正
- 問題：`parse_transactions_taishin` 跑出 0 筆交易
- 根因：
  - 日期 pattern `^\d{2}/\d{2}/\d{2}` 不符民國 3 碼年份 → 改 `^\d{3}/\d{2}/\d{2}`
  - desc 與 date/amount 列 Y 差 0.0074，原 tol=0.007 不夠 → 改 0.012
  - 金額欄 x≈0.55，原抓 x>0.80 → 改 0.45<x<0.70
  - start 判斷誤觸 disclaimer 中的「消費明細」 → 改為同一列同時含「消費日」AND「新臺幣金額」才算 header
- 結果：202604 跑出 1 筆（遠傳 NT$2,058）

### 2. Sales Report 全自動化
- **generate_report.py 重寫**：格式對齊 SalesAnalysis_20260403 舊檔
  - `write_hierarchical_pivot()`：地區別→國家別→客戶簡稱 3 層，L0 深藍/L1 淺藍/總計黃
  - `write_product_order_pivot()`：機種別小計 + 分類別小計兩段
  - 加入 `--sor/--prod/--output` CLI 參數供自動化呼叫
- **sales_report_processor.py 新增**（`~/Documents/MyClaude/`）
  - Gmail 觸發：from:hasnashih@gmail.com + subject "sales report YYYYMMDD"
  - 自動下載附件 → generate_report → 寄雙方 → TG → 貼標籤「已處理sales report」
- **LaunchAgent 排程**：`com.kuochengchen.sales-report`，週一至週五 10/12/15/18 點
- **Skill**：`~/Documents/MyClaude/skills/sales-report.md`
- **Git**：`kuozheng10/sales-report-analysis` 已 push

### 3. SOP 筆記
- 存：`03.科技工具/ai-agent-modular-architecture.md`

## 關鍵洞察
- macOS LaunchAgent `StartCalendarInterval` 支援 array，可列舉多個 weekday×hour 組合
- `generate_report.py` 的 globals 在 `if __name__ == '__main__':` block 中賦值後，函式內的 global lookup 可正確取得新值
- xlwings 需要 Excel.app，本機沒裝，只能用 openpyxl 靜態模擬樞紐格式
